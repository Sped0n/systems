# Todos

Provides the `todo` tool and `/todos` TUI for file-backed project tasks under
`.pi/todos` by default. Tasks support creation, updates, notes, assignment,
release, closure, deletion, and interactive selection.

Set `PI_TODO_PATH` to override the storage directory. Automatic garbage
collection is disabled by default.

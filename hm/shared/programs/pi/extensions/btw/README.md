# BTW

Provides `/btw` as a side-channel conversation that answers a question without
interrupting the main agent thread. It keeps its own in-memory session, renders
an overlay in the TUI, and can summarize the side thread back into the session.
